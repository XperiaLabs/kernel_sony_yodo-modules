ifeq ($(CONFIG_AKS_ADC_JOYSTICK),m)
KBUILD_CPPFLAGS += -DCONFIG_AKS_ADC_JOYSTICK
endif

ifeq ($(CONFIG_AKS_ADS1015),m)
KBUILD_CPPFLAGS += -DCONFIG_AKS_ADS1015
endif

obj-$(CONFIG_AKS_ADC_JOYSTICK) += aks-adc-joystick/
obj-$(CONFIG_AKS_ADS1015) += aks-ads1015/
